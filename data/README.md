# Donnees locales

`datasante_demo.db` est genere localement par `python -m scripts.init_demo_db` et reste ignore par Git. Les mesures qu'il contient sont entierement synthetiques, agregees et deterministes. Elles ne representent ni des patients ni des valeurs publiees par Data Ameli.
